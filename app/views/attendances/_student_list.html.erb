<%= render partial: 'list_students', locals: {header: 'Checked in:', list: @present, override_state: 'absent'} %>

<%= render partial: 'list_students', locals: {header: 'Not checked in:', list: @absent, override_state: 'present'} %>
