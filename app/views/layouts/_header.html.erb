<nav class="navbar navbar-inverse">
<div class="container">
  <div class="navbar-header">
    <strong> <%= link_to 'Attendeis', root_path, class: 'navbar-brand' %> </strong>
  </div>
  <div id="navbar">
    <% if current_user %>
    <ul class="nav navbar-nav pull-right">
        <li> <%= link_to 'Admin', rails_admin_path if current_user.is_admin? %></li>
        <li> <%= link_to "Sign out #{current_user.name}...", logout_path %> </li>
    <ul>
    <% end %>
  </div>
</div>
</nav>
